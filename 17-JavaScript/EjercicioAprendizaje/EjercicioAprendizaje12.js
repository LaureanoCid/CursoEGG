/*
    Escribir una función flecha de JavaScript que reciba un argumento y retorne el tipo de 
    dato.
*/
let tipoDeDato = (variable1) => document.write(`El caracter es: ${typeof(variable1)}`);
tipoDeDato(true);